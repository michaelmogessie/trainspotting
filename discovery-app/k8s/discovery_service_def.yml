apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  namespace: michaelmogessie
spec:
  type: NodePort
  selector:
    app.name: eureka
  ports:
    - port: 8484
      targetPort: 8484
      nodePort: 30030